# project-local tooling
node_modules/
.npm-cache/
.cache/

# local configuration (media roots, port) and the record of what was imported from the camera
config.json
import-ledger.json
import-ledger.json.tmp

# optional full-length sample files downloaded by `npm run samples` (the trimmed test fixtures are committed)
samples/

# OS / editor
.DS_Store
*.log
