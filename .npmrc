# Keep everything project-local: npm cache lives inside the project, nothing is written to ~/.npm.
cache=.npm-cache
fund=false
audit=false
# Dev tooling (eslint) is part of the project even when the shell sets NODE_ENV=production.
include=dev
